name: Build & Release on Tag

on:
  push:
    tags:
      - 'v*'   # 匹配 v1.0.0

jobs:
  build-and-publish:
    strategy:
      fail-fast: false
      max-parallel: 1
      matrix:
        include:
          # macOS
          - os: macos-latest
            arch: arm64
            build_script: build:mac

          # Linux
          - os: ubuntu-latest
            arch: x64
            build_script: build:linux
          - os: ubuntu-latest
            arch: arm64
            build_script: build:linux

          # Windows
          - os: windows-latest
            arch: x64
            build_script: build:win

    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm install

      # Linux 构建
      - name: Build & Publish (Linux)
        if: matrix.os == 'ubuntu-latest'
        shell: bash
        run: |
          echo "Building target: ${{ matrix.os }} / ${{ matrix.arch }}"

          # electron-builder 支持 --arm64 --x64
          npm run ${{ matrix.build_script }} -- \
            --${{ matrix.arch }} \
            --publish always
        env:
          GH_TOKEN: ${{ secrets.GH_PAT }}

      # macOS 构建
      - name: Build & Publish (macOS)
        if: matrix.os == 'macos-latest'
        shell: bash
        run: |
          echo "Building target: ${{ matrix.os }} / ${{ matrix.arch }}"

          npm run ${{ matrix.build_script }} -- \
            --${{ matrix.arch }} \
            --publish always
        env:
          GH_TOKEN: ${{ secrets.GH_PAT }}
          CSC_LINK: ${{ secrets.CSC_LINK }}
          CSC_KEY_PASSWORD: ${{ secrets.CSC_KEY_PASSWORD }}
          APPLE_ID: ${{ secrets.APPLE_ID }}
          APPLE_APP_SPECIFIC_PASSWORD: ${{ secrets.APPLE_APP_SPECIFIC_PASSWORD }}
          APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}

      # Windows 构建
      - name: Build & Publish (Windows)
        if: matrix.os == 'windows-latest'
        shell: powershell
        run: |
          Write-Host "Building target: ${{ matrix.os }} / ${{ matrix.arch }}"
          npm run ${{ matrix.build_script }} -- --${{ matrix.arch }} --publish always
        env:
          GH_TOKEN: ${{ secrets.GH_PAT }}
